class Developer {

    // properties - fields
    String first
    String last
    def languages = [] // array list
    // will discuss def further down the line

    // remember all methods are public by default
    void work(){
        println "$first $last is working...!"
    }
}
